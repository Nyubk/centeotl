                         ┌──────────────────────────────┐
                         │         Usuario/App           │
                         └──────────────┬───────────────┘
                                        │
                  ┌─────────────────────┴────────────────────┐
                  │              Flutter UI                   │
                  │ (Android, macOS, Web offline)             │
                  │ - Pantallas: Carga, Resultados, Gráficos  │
                  │ - Navegación y controles                  │
                  └───────┬───────────────────────┬──────────┘
                          │                       │
                          │                       │
      ┌───────────────────┘               ┌───────┴────────────────┐
      │ OCR (Google ML Kit)                │ Motor Estadístico R    │
      │ - Reconocimiento en imágenes       │ - Paquetes:            │
      │ - Preprocesamiento (stringr)       │   Importación: readr, readxl, jsonlite
      │ - Conversión a tabla CSV           │   Limpieza: dplyr, tidyr, lubridate
      └──────────┬──────────────────────── │   Estadística: anthropometry, anthro,
                 │                          │                car, stats, rstatix, Hmisc
                 │                          │   Visualización: ggplot2, plotly, ggpubr
                 │                          │   Reportes: rmarkdown, knitr, officer
                 │                          │ - Procesos:
                 │                          │   1. Cálculos antropométricos
                 │                          │   2. ANOVA, t-test, percentiles, z-scores
                 │                          │   3. Interpretación textual automática
                 │                          │   4. Generación de gráficos
                 │                          │   5. Exportación (CSV, XLSX, PDF)
                 │                          └───────────┬──────────┘
                 │                                      │
          ┌──────┴──────────────────────────────────────┴──────┐
          │     Almacenamiento Local (sin conexión a internet) │
          │  - Archivos cargados                                │
          │  - Resultados y gráficos generados                  │
          │  - Exportaciones finales                            │
          └────────────────────────────────────────────────────┘
